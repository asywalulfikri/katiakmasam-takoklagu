<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.tebaklaguminang.asywalul.tebaklagu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/kaset_pita"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.PassActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme"
            android:label="@string/title_activity_pass"/>
        <activity
            android:name=".ui.InterestialLogout"
            android:label="@string/title_activity_pass"/>

        <activity
            android:name=".ui.GuideActivity"
            android:label="@string/title_activity_guide"/>

        <activity
            android:name=".ui.Cara_Main"
            android:label="@string/title_activity_guide"/>

        <activity
            android:name=".ui.InterestialLogout2"
            android:label="@string/title_activity_guide"/>

        <activity
            android:name=".ui.Perantara"
            android:label="@string/app_name" />

        <activity android:name=".ui.MainActivity2"/>

    </application>

</manifest>